# アーキテクチャ — Houdini Bridge MCP

セキュリティ最優先の、データ専用の制御サーフェスです。AIエージェントがMCPを介して、Houdiniが行えることのほとんどにわたってHoudiniを駆動できるようにします — そこでAIは*検証済み*のラングルを記述するのであって、任意コードを書くことは決してありません。カタログは、ジオメトリ＆クリーンアップ、インスタンシング、シミュレーション、キャラクターのリギング＆アニメーション、COP/画像、ルック＆マテリアル、ML/ONNX、SideFX-Labs、レンダー/エクスポートにまたがり、加えて独自の**現実世界の地理空間データレーン**（DEM / 3DEP / LIDAR）を備えます。これは標高を再投影し、真のスケールで配置するもの — 数ある強力なレーンの1つであって、これがすべてではありません。Windows優先。**ターゲット：Houdini 21.0.671**（H22は安定性待ちのため未採用）。

## 設計原則
1. **フィルターによるのではなく、構造によるセキュリティ。** このツールは、固定された、型付きの、*データ専用*のサーフェスを公開します。任意コード実行がHoudiniに到達することはありません — Python-execも、汎用ノードドライバーも、生のVEXもありません。Houdiniの埋め込みPythonは、完全で無制限のCPython（ホストのRCE）であるため、唯一の防御は*何がそこに到達しうるか*を制約することです。唯一のコードを運ぶツール — `set_attrib_expr`、オプトインで**デフォルトで無効** — も例外ではありません：これはVEXアトリビュートスニペットを受け取り、エグゼキューターが*Houdiniがそれを目にする前に許可リストに対して検証します*（許可リスト化された関数のみ、ファイル/ホスト/ネットワークI/Oなし、証明可能に全域的 — 常に停止します）。したがってこれは境界付きの入力部分集合であり、生のVEXや任意コード実行の経路では決してありません。条件分岐に加えて静的に境界付けられたカウントループが許可されます。オプトインのループ（`allow_attrib_loops`）と削除専用のトポロジー編集（`removepoint`／`removeprim`、`allow_attrib_geoedit`）は、それぞれが独自のデフォルトで無効の同意の背後にあります。
2. **ブリッジは配線し、人間が実行する。** レンダーグラフは完全に構築されますが、エージェントによって実行されることは決してありません — レンダーはユーザーがトリガーします。これにより、リソースのDoS、意図しない出力、重いシーンのフリーズを取り除きます。
3. **1つの作業ディレクトリ。** すべてのファイルの読み書きは、単一のユーザー設定のルートに`realpath`で制限されます。そこから逃れるものは何もありません。
4. **監視され、監査可能。** エージェントが行うすべての呼び出しのライブログが、GUIで表示されます。

## トポロジー

```
  AI / MCP client            gateway (Rust + GUI)                 executor (Python, in Houdini)
  ───────────────  stdio   ───────────────────────  loopback   ─────────────────────────────
  Claude / Cursor  ─────▶   • sole entry point         HTTP/     • hwebserver in the live session
                            • typed, schema-validated  pipe      • fixed table of bounded ops
                            • serialized 1-at-a-time              • no exec / node_op / raw VEX
                            • working-dir confinement             • main-thread-marshalled hou.*
                            • LIVE AUDIT LOG (GUI)                • wire-only render
```

ゲートウェイは玄関口であり、エグゼキューターの*機能集合*がセキュリティ境界です。LLMがネットワークポートに触れることは決してありません — LLMはゲートウェイに対してstdio MCPで通信します。

両半分とも構築済みです：ゲートウェイは手作りのMCP stdioサーバー（stdin/stdout上の改行区切りのJSON-RPC 2.0）であり、コンパイル時に型付けされたツールカタログを駆動し、タブ付きランチャーGUIとともに出荷されます。エグゼキューターは稼働中のHoudiniセッション内で動作し、手動のスニペットなしで起動時に自動arm（アーム）されます。arm / ホットリロード / 作業ディレクトリ制限がどのように連携するかについては、下記の**ランタイムメカニズム**を参照してください。

## コンポーネント

### `houdini_executor/` — Houdini内Pythonエグゼキューター
- `hwebserver`（ループバック）を介してHoudini内で動作します。完全な`hou.*`アクセスを持ちますが、型付きで検証済みのハンドラーからなる固定レジストリのみを公開します。
- すべてのハンドラー：数値/列挙値/許可リスト化されたパスの入力、クランプされた数値、リクエストサイズの上限。
- パスの制限：すべての読み書きパスは`realpath`を介して解決され、単一の設定された作業ディレクトリのルートに対してチェックされます。
- `hou.*`のためのメインスレッドマーシャリング（Houdiniのシーンモデルはシングルスレッドです）。
- `handlers/` — ワークフローの各ステージにつき1モジュール（下記）。

### `gateway/` — Rust製MCPサーバー＋GUI（唯一のLLMエントリポイント）
- **手作りのMCP stdioサーバー** — stdin/stdout上の改行区切りのJSON-RPC 2.0（SDK依存なし）。`initialize` / `tools/list` / `tools/call`を処理します。STDOUTはプロトコル専用に予約されています。エグゼキューターのオペレーションを、コンパイル時カタログ（`tools.rs`）から型付きMCPツールとして公開し、各ツールがその`Param`を一度だけ宣言するため、スキーマ＋検証＋ディスパッチが1つの宣言から導出されます。ユニットテスト済み（`#[cfg(test)] mod tests`）。
- **タブ付きランチャーGUI**（eframe） — **Status**、**Working dir**、**Audit log**、**Data sources**、**Settings**タブを備えた左ナビゲーションのアプリで、加えて**Armed**ステータスピル（arm時はオレンジ）を備えます。Working-dirタブの**Apply**は制限ルートを`arm.json`に書き込みます。Settingsは**Auto-arm Houdini**トグルと**Install Houdini package**アクションを備えます。
- パス/URLの許可リスト化と型付きスキーマ検証はここで強制され、stdioトランスポートへの**8 MBのインバウンドフレーム上限**（メモリDoS対策で、単一の終端されていない行が無制限にバッファリングできないようにするもの）も同様です。トークン認証とエグゼキューター自身の1 MBリクエストボディ上限はエグゼキューター側です（下記の*エグゼキューターコア*を参照）。リクエストは1件ずつディスパッチされます（直列化）。レートリミッターはありません — 単一クライアントのstdioパイプ（唯一のAIクライアント）越しでは、レート制限は実質的な保護を加えず、直列化がすでに並行性を境界付けます。ゲートウェイは玄関口です — エグゼキューターの機能集合がセキュリティ境界です。
- `arm.json`は`working_dir` / `port` / `token`の唯一の信頼源です。`config.rs`のリゾルバー（`resolve_working_dir` / `resolve_executor_port` / `resolve_token`）はこれをライブで（mtimeキャッシュで）読み取るため、ゲートウェイとエグゼキューターは常に同じ制限ルートとエンドポイントで一致します。

### `downloader/` — 信頼できるソースからのデータ取得
- 信頼できるソースの許可リスト（USGS 3DEP / TNM Access、州のDEMポータル、グローバルDEMなど）からのDEM/3DEPのプロベナンスゲート付きフェッチ。マルチ解像度（3DEP + DEM）。任意URLのインポートはありません。
- ソースアダプター群（共有の`fetch`/`net`/`sources`コアに加えてポータルごとのモジュール）として実装され、acquireステージに供給します。ソースの許可リストが境界です — 設計とその信頼できるソースの解決は確定済みです。

## エンドポイントサーフェス — 11のワークフローステージにまたがる1,467の型付きで検証済みのツール
1. Acquire/Import · 2. Terrain（ハイトフィールド） · 3. Model/Geometry · 4. Cleanup/Mesh（+VDB） ·
5. Instance/Scatter · 6. Sim（レシピテンプレート化） · 7. Look（マテリアル/ライト/カメラ） ·
8. Render/Export（レンダー＝ワイヤー接続のみ、エクスポート＝実行可能で制限あり） · 9. Analysis（テンプレート化されたVEXライブラリ） · 10. Scene/utility · 11. Weather/Atmospherics/Ocean。

## ランタイムメカニズム

両半分がどのようにarmし、同期を保ち、ホットに反復するか：

- **自動arm（手動スニペットなし）。** Houdini 21のパッケージ（`houdini_package/` → ユーザーのpref dir）が起動スクリプトを実行し、UIが立ち上がったら`~/.houdini-bridge-mcp/arm.json`を読み取り、`enabled`であれば — エグゼキューターを`sys.path`に載せて`server.auto_arm()`を呼び出します。`auto_arm()`は`working_dir` / `token` / `port`を設定から再読み込みし（*ユーザー*がHoudiniを起動したためゲートウェイのenvは存在しません）、armします。欠落した/無効化された設定はサイレントなno-opであり、起動が滞ることは決してありません（グラフィカルな`hwebserver.run(...)`は即座に返ります）。GUIの**Install Houdini package**アクションはパッケージを所定の場所にコピーします。**Auto-arm**トグルは`enabled`を切り替えます。
- **単一ソースの作業ディレクトリ。** `arm.json`の`working_dir` — GUIの**Apply**によって書き込まれる — が制限ルートであり、**両**側によって*ライブ*で（mtimeキャッシュで）読み取られます：エグゼキューターは`_effective_working_dir()`を介して、ゲートウェイは`config.rs::resolve_working_dir`を介して。すべてのパスは`realpath`で解決され、そのルートの下に位置することが要求されます（サブディレクトリは辿れます。外部にあるものは例外を発生させます）。設定が欠落/読み取り不能/無効の場合、両者ともarm時のルートにフォールバックするため、制限がサイレントに開放されることは決してありません。
- **ルーティング — 1つのプレフィックスハンドラー。** hwebserverのパターンは正確一致/プレフィックス一致であり、正規表現ではありません。エグゼキューターは、ちょうど1つのプレフィックスのキャッチオール`hwebserver.urlHandler("/tool", is_prefix=True)`と、`/health`ルートを登録します。ハンドラーは`/tool/`プレフィックスを取り除き、`_dispatch(name, request)`を呼び出します。これは*リクエスト時に*`_REGISTRY`内でハンドラーを検索します。ツールの追加＝レジストリエントリの追加であり、ルートの再登録はありません（ルートは`hwebserver.run()`でロックされます）。
- **ホットリロードの開発ループ。** `POST /tool/reload`はすべての`houdini_executor.handlers.*`モジュールをその場でリロードし、それらの`@endpoint`デコレーターを再実行して`_REGISTRY`エントリを上書きします — そのためコードの修正は**Houdiniを再起動せずに**有効になります。単一の`/tool`キャッチオールがリクエストごとに`_REGISTRY`から解決するため、ルートを再登録する必要はありません。`_REGISTRY`を所有する`server`モジュール自身はリロードされることはありません。

## ビルドステータス

- **エグゼキューターコア — 構築済み。** サーバーのブートストラップ、認証（`X-HMCP-Token`、定数時間比較）、`realpath`によるパスの制限、メインスレッドマーシャリング（`run_on_main`）、1 MBのリクエスト上限、ハンドラーレジストリ、ホットリロード、そして証明エンドポイント（health、scene_info）。データ専用：execなし、汎用ノードドライバーなし、生のVEXなし。
- **完全なエンドポイント集合 — 構築済み。** 11のワークフローステージにまたがる1,467の型付きで検証済みのツール、`houdini_executor/handlers/`内（ステージごとに1モジュール）。
- **Rust製ゲートウェイ＋GUI — 構築済み。** 手作りのMCP stdioサーバー＋コンパイル時に型付けされたカタログ（`tools.rs`、ユニットテスト済み）＋タブ付きeframeランチャー（Status / Working dir / Audit log / Data sources / Settings、Armedピル、Install-package + Auto-arm）＋`arm.json`単一ソース設定。
- **ダウンローダー — 実装済み。** 信頼できるソースからのDEM/3DEP取得。設計とソースの許可リストは確定済み。
- **ドキュメント＋正直なセキュリティREADME＋パッケージング** — 出荷可能なHoudini起動パッケージとこのドキュメント。
