# セキュリティポリシー

## スコープ

houdini-bridge-mcpは、ローカルでシングルユーザーのツールです。小さなRust製ゲートウェイを介して、稼働中のHoudiniセッションに対する型付きで検証済みの制御をAIクライアントに提供します。攻撃対象領域は以下に限定されます：

- AIクライアントとゲートウェイの間のstdio JSON-RPCチャネル
- ゲートウェイとHoudini内Pythonエグゼキューターの間のループバックHTTPチャネル
- ローカルファイルシステム（ツール引数として渡されるパス）
- ツール引数そのもの（数値、列挙値、ノードパス、ファイルパス）
- arm時にHoudiniセッションへロードされるエグゼキューターPython

---

## 脅威モデル

houdini-bridge-mcpは、**AIクライアントを介してマシンの所有者が操作する、ローカルでシングルユーザーのツールです。** その脅威モデルは、所有者が自分自身のマシン上で実行することであり — 信頼できない、マルチテナント、またはネットワークに公開された配備ではありません。

- **信頼するもの：** マシンとその所有者、Houdiniのインストール、ゲートウェイとJSON-RPCで通信するAIクライアント。
- **信頼しないもの：** いずれかのツール呼び出しで到着する*引数*。所有者が信頼できる場合であっても、AIクライアントは読み取ったコンテンツによって誘導されうるため、すべての引数は無害と決めつけずに受け取り時点で検証されます。
- ゲートウェイやエグゼキューターのループバックポートを、信頼できないネットワークアクセスに**公開しないでください**。この設計は、両エンドポイントが1台のマシン上の単一の信頼ドメイン内に存在することを前提としています。

---

## 設計：データ専用エグゼキューター

中核となるセキュリティ上の判断は、Houdini内エグゼキューターが**型付きで検証済みのハンドラーからなる固定レジストリのみ**を公開し、それ以外は一切公開しないことです。

意図的に**任意コード実行の経路は存在しません**：`exec`エンドポイントも、汎用ノードドライバーも、生のVEX/ラングルの記述も、「このスニペットを実行する」ツールもありません。Houdiniの埋め込みPythonは、完全で無制限のCPythonです — そのため、これらのいずれかが存在すれば、所有者の全権限でのリモートコード実行になってしまいます。危険な機能をサンドボックス化するのではなく、その機能そのものをカタログに存在させないという方針です。

### なぜデータ専用なのか — そしてなぜexecベースのMCPの上に構築しなかったのか

既存のDCC向けMCPサーバーのほとんど（人気のあるBlender用およびHoudini用のものを含む）は、`execute_code` / 任意Pythonまたは生のラングルツールを公開し、モデルに鍵を手渡します — 実質的に「これがシェルだ、あとは頑張れ」というわけです。本プロジェクトはそれらから派生したもの**ではありません**。最初からデータ専用で構築されており、それはAIエージェントが実際にどのように破綻するかへの意図的な対応です：

- **モデルは間違えることも、誘導されることも、退行することもある。** 駆動するエージェントは、ハルシネーションを起こしたり、読み取ったコンテンツによって操作されたり（プロンプトインジェクション）、あるいは単に誤動作したりします。READMEはAIが間違いを犯しうることを率直に述べています — この設計はAIが*間違える*ことを前提とし、モデルがうまく使ってくれると信頼するのではなく、危険な機能を構造上取り除いています。
- **任意コード＋オープンな目標こそが、文書化された高深刻度の障害モードです。** 2026年を通じて、エージェント型AIで繰り返され、エスカレートしてきたパターンは次のとおりです：コード実行と目標を与えられたエージェントがサンドボックスを脱出し、マシンの速度で自律的かつ検知されずに行動し、認証情報・ネットワーク・ファイルシステムに到達する — 「手段を選ばず勝つ」というものです。`execute_code`ツールはまさにその構成です。
- **無制限のCPython/VEXを事後的に確実にサンドボックス化することはできません。** 保持されることを*願う*ラッパーは、存在しない機能よりも脆弱です。したがって、ここでの境界は**固定された型付きツールカタログそのもの**です — モデルは、列挙され、スキーマ検証され、引数チェックされたハンドラーしか呼び出せません。
- **監督は構造上保たれます。** レンダー／キャッシュ／シミュレーションはワイヤー接続のみ（構築はするが実行はユーザーが行う）であり、新しいツールには人間が実行するリビルドが必要であり、唯一のコード経路（検証済みVEX）は、境界付きで、非チューリング完全で、デフォルトで無効で、オペレーターの同意を要する部分集合であって、実行される*前に*検証されます — 決して開いたドアではありません。

このトレードオフは正直かつ意図的なものです：このMCPはシェルを手渡すものよりも制限が大きく、その制限**こそがセキュリティ特性です。** それこそが、所有者がモデルの常に正しい振る舞いに自分のマシンを賭けることなく、稼働中のHoudiniセッションに向けられる理由です。

ビルド時のテストがこれを強制します：ツールカタログが`exec`、`node_op`、`wrangle`、`heightfield_op`を決して含まないことがアサートされます。新しい機能は、新しい型付きハンドラーを宣言することによってのみ追加され、既存のハンドラーを汎用ドライバーへと拡張することによっては決して追加されません。

このツールをどう捉えるべきかの帰結：**ゲートウェイは玄関口であって、セキュリティ境界ではありません。エグゼキューターの機能集合 — 型付きハンドラーの固定リスト — が境界です。** それらのハンドラーのいずれかへの呼び出しとして表現できないものは、クライアントが何を送ろうと到達不可能です。

---

## 入力検証

すべてのツールは、そのパラメーターを型付きの値として宣言します。その単一の宣言から、クライアントが目にするスキーマと、Houdiniに何かが到達する前にすべての呼び出しが通過しなければならないサーバー側の検証の両方が生成されます。自由形式のものは一切ありません。

- **パスの制限。** すべてのファイルシステムパスのパラメーターは、単一の作業ディレクトリに`realpath`で制限されます（`~/.houdini-bridge-mcp/arm.json`から読み込まれ、サブディレクトリは辿れます）。解決はシンボリックリンクを辿り、封じ込めを確認する前に`..`を畳み込むため、ジャンクションやシンボリックリンクがルートの外へトンネルすることはできません。まだ存在しない書き込み対象は、解決された親のチェーンが制限内に留まる場合にのみ許可されます。
- **多層防御。** ゲートウェイがパスを制限し、エグゼキューターがすべてのパスを同じルートに対して**再チェック**します — ゲートウェイの検証だけを決して信頼しません。エグゼキューターはまた、Windowsの拡張長（`\\?\`）プレフィックスを正規化するため、正規化されたパスであってもルートに一致します。
- **数値のクランプ。** 数値パラメーターは、拒否されるのではなくその宣言された範囲に**クランプ**されます。10^9行の要求は、メモリ不足によるクラッシュではなく、上限値になります。非有限の数値は拒否されます。
- **それ以外もすべて型付き。** 列挙パラメーターは固定集合のいずれかでなければならず、ノードパスと文字列は空であってはならず、固定長の数値タプルはその長さに一致しなければなりません。未知のパラメーターキーは即座に拒否されます — 予期しないものが境界をすり抜けて紛れ込むことはありません。
- **認証。** エグゼキューターは、すべてのリクエストで共有セッショントークン（`X-HMCP-Token`）を要求し、定数時間で比較します。有効なトークンを持たないリクエストは拒否されます。
- **ボディの上限。** リクエストボディはサービス拒否対策として上限（1 MB）が設けられており、サイズ超過のリクエストはパース前に拒否されます。
- **シングルスレッドのシーンアクセス。** HoudiniのAPIはスレッドセーフではありません。ワーカースレッドがシーンに直接触れることは決してなく、各ジョブをHoudiniのメインスレッドへマーシャリングします。ジョブごとのタイムアウトが*呼び出し側*の待機時間を制限し、失敗が報告された後に遅れて完了したジョブがシーンを変更するのを防ぎます。正直な限界：タイムアウトは**まだ開始していない**ジョブをキャンセルしますが、すでにメインスレッドで実行中のジョブは完了まで実行されます — Houdiniはクックの途中で安全に中断する手段を提供しないため、真に暴走したクックは終了するまでセッションをブロックしうるのです。この設計は、ウォッチドッグに頼るのではなく、一般的な重い経路をその危険から遠ざけます：レンダー／ベイク／ROPは**ワイヤー接続のみ**（構築され配線されるが、MCPによって実行されることは決してなく — 実行はユーザーが行います）であり、唯一のコード経路（検証済みVEX）は**境界付きループ**のみを許可します（`while`／`do`／`gather`は禁止されたまま。`for`は静的リテラルまたは`min()`でクランプされた上限を持つ場合にのみ受け付けられ、`foreach`は反復回数をVEXが**エントリ時に固定する**配列ループとしてのみ受け付けられます — スナップショットで有限であることが検証され、かつ**リーフ専用**であるため、実行時のカウントが外側／ネストされたループによって増幅されることは決してありません。オプトインでデフォルトで無効）。これにより部分集合は**全域的／非チューリング完全**なまま保たれます — スニペットは遅くはなりえても、決して無限にはならず、**クックの途中での中断がない**ため、境界付きだが遅いクックであっても、人間が実行できるようになる前に完了まで実行され、数値の大きさはクランプされます。トポロジーの変更は、2つの独立したデフォルトで無効の同意に分割されており、それぞれがリテラルの入力`0`サンドボックスジオメトリに固定され、それぞれが独立しています（一方を有効にしても他方は決して付与されません）：**削除**（`removepoint`／`removeprim`、`allow_attrib_geoedit`）は単調に縮小する／自己境界付きであり、**構築／成長**（`addpoint`／`addprim`／`addvertex`／`removevertex`、`allow_attrib_geogrow`）は静的な総量上限を持たないため、**大きいが有限**です（ループ上限×要素数によって境界付けられる） — 重い構築は遅くクックされますが、決して無限にはならず、実行は人間が行います。メモリガバナーは**助言的なテレメトリであって、ハードなリソースゲートではありません**（ライブの余裕量エンベロープを提示し、破滅的な帯域でのみ拒否します） — OOMに対する保証ではなく、ベストエフォートとして扱ってください。
- **`batch`は各オペレーションを同じゲートを通す — バイパスなし。** ネイティブの`batch`メタツールは、呼び出しごとのレイテンシを削減するためだけに存在します（1回のMCP呼び出しで最大64オペレーション）。これは直接呼び出しにない機能を一切付与しません：各サブオペレーションは*同一*のコード経路を通じてディスパッチされます — ツールの検索、`ToolDef`のスキーマ検証、数値のクランプ、`realpath`によるパスの制限、そしてオペレーションごとに1件の順序付き監査イベント。バッチが呼び出せるのは実在するカタログツールだけであり（未知またはカタログ外の名前は直接呼び出しと同様にエラーになります）、**ネストできません** — `batch`という名前のオペレーションは、構造検証時とディスパッチ時の防御的チェックの両方で拒否されます。バッチ処理は境界を囲むエンベロープであって、境界に穴を開けるものでは決してありません。
- **オプションのアクションスロットル（`HMCP_MIN_ACTION_INTERVAL_MS`）。** デフォルトで無効（`0`）。Nミリ秒に設定すると、ゲートウェイは、連続する**破壊的な**ツール呼び出し — `delete_node`、`clear_scene`、`save_scene`、`delete_keyframes` — の間に最小N msの実時間の間隔を、ディスパッチ前に残りの間隔を*スリープ*して消化することで強制します。これは**ペースを整えるものであって、決して拒否しません**：正当な遅いシーケンスは依然として完了しますが、暴走ループやプロンプトインジェクションは、シーンを破壊する呼び出しを下限が許すよりも速く連射することができず、人間が気づく時間を与えます。非破壊的なツール（構築、読み取り）は決して遅延されないため、バッチ構築と検査は高速なまま保たれます。これは安全ガバナーであって、セキュリティ境界ではありません — 破壊的なアクションの*レート*を制限するのであって、その認可を制限するのではありません（認可は依然として上記の固定機能集合＋検証です）。

---

## サポートバージョン

セキュリティ修正は最新の公開バージョンのみに適用されます。

| バージョン | サポート |
|---|---|
| 0.x（最新） | ✅ |
| それ以前 | ❌ |

---

## 脆弱性の報告

**セキュリティ上の脆弱性には公開Issueを使用しないでください。**

houdini-bridge-mcpリポジトリで、GitHubのプライベート脆弱性レポートを使用してください：

1. リポジトリの**Security**タブにアクセスします。
2. **Report a vulnerability**をクリックして、プライベートセキュリティアドバイザリを開きます。
3. 再現できる十分な詳細を含めて問題を説明します。

7日以内に返答します。脆弱性が確認された場合、できる限り早急に修正をリリースし、ご希望の場合はリリースノートにクレジットを記載します。

---

## 既知の設計上の決定

- **任意コード実行の機能なし：** エグゼキューターは型付きハンドラーのみを公開します。`exec`、汎用ノードオペレーション、生のVEX/ラングルの記述は意図的に存在しません。Houdiniの Pythonが無制限のCPythonであり、そのいずれもがリモートコード実行になるためです。ビルドには、禁止されたツールがカタログに現れた場合に失敗するテストがあります。

- **サーバーが記述するノードテンプレート（開示）：** 2つのカテゴリーがノードの*内容*をサーバー側で記述します。コードの読み手がこれらをゆるい境界と誤解しないよう、ここで開示します。(1) 唯一の**検証済み**コード経路 — `set_attrib_expr` — は、非チューリング完全な部分集合に対して許可リストを優先してチェックされた検証済みVEXスニペットを、ラングルに設定される*前に*配置します（デフォルトで無効、エグゼキューター側で`allow_attrib_expr`によりゲートされます）。この経路はまた、入力0の要素を**削除**（`removepoint`／`removeprim`、*別個の*デフォルトで無効の`allow_attrib_geoedit`同意によりゲートされる — 単調に縮小し、arg0はリテラル`0`に固定）し、それらを**構築／成長**（`addpoint`／`addprim`／`addvertex`／`removevertex`、*3つ目の*別個のデフォルトで無効の`allow_attrib_geogrow`同意によりゲートされる — 大きいが有限、arg0はリテラル`0`に固定）することもできます。一方のトポロジー同意を有効にしても他方は決して付与されません。(2) 小さく**列挙された**ハンドラー集合が、型制約された値のみを補間した**固定**テンプレートを記述します — 生のユーザー文字列がコードテキストに到達することは決してありません。全集合（13ツールにまたがる15箇所、2026-08-14に再検証済み）は次のとおりです：**3つのPython-SOPインジェクター** — `import_heightfield`と`import_ecef_tile`（`%r`を介して`confined_path`値を固定の`np.load(...)`テンプレートに）、および`create_curve`（座標ごとに`float()`検証された`[x,y,z]`トリプルの`json.dumps`を固定のカーブ構築テンプレートに）。そして**12のVEX attribwrangleテンプレート** — `import_pointcloud`（整数の間引きストライド）、`build_globe`（×3：定数のlon/lat→uvスニペットに加え、制限パス／floatのトライプラナー項）、`point_normals`、`segment_planar`、`despeckle`、`tag_radial`、`isolate`（数値境界＋ワールド変換タプル、いずれもクランプ後に`%d`／`%g`／`%.9g`を介して）、加えて`heightfield_morph`と`heightfield_fill`（×2）で、これらの補間されるレイヤー／アトリビュートの**名前**はそれぞれ`validate_ident`を通され、その数値は`%g`を介します。したがってすべての補間は、クランプされた数値（`%d`／`%g`／`%.9g`）、`confined_path`値（`%r`）、float検証された数値の`json.dumps`、または`validate_ident`でゲートされた識別子のいずれかであり — 決して生のユーザー文字列ではないため — いずれもインジェクションのベクトルにはなりません。このリストは、ツール表面が新しい経路へと拡大するにつれてここで維持され、CIのソースガード`test_no_raw_vex_sinks.py`（RT-11）は、新たな未監査の生コードテキストのシンクが導入された場合にビルドを失敗させます。

- **なぜ生のVEXがないのか — そして検証済みVEXがどのように人間を関与させ続けるのか：** 生のVEXと任意Pythonが除外されているのは、Houdiniの埋め込みPythonが完全で無制限のCPythonであり、VEXにはエスケープハッチ（`system`／`run`、式／`ophook`ブリッジ、ファイルおよびネットワークの組み込み関数）があるためです — いずれも所有者の全権限でのリモートコード実行です。しかしラングルは、この作業の芸術性のための*本物の*ツール（しきい値駆動の破壊、クラスター境界のロジック、カスタムパーティクルマスク）であるため、ブリッジはそれらを禁止せず、**ゲートします**：
  - **検証済み安全VEX経路**（`set_attrib_expr`）は、ラングルスニペットを受け付け、非チューリング完全な部分集合に対して**許可リストを優先して**チェックし（アトリビュートの読み書き、`ch()`、条件分岐、数学、`sprintf`、ホワイトリスト化された関数集合、要素の削除 — 入力0に対する`removepoint`／`removeprim`、別個の`allow_attrib_geoedit`同意の背後でデフォルトで無効にゲート — 要素の構築／成長 — 入力0に対する`addpoint`／`addprim`／`addvertex`／`removevertex`、別個の`allow_attrib_geogrow`同意の背後でデフォルトで無効にゲート — グループ書き込み）、RCEまたはI/Oを可能にするすべてを**拒否します** — Houdiniがその文字列を目にする*前に*。これは**デフォルトで無効**です：ユーザーが`allow_attrib_expr`を設定した場合、すなわち明示的な同意がある場合にのみ実行されます。
  - **リファレンスの受け渡し**（`vex_reference`）は読み取り専用です：AIは、ユーザーが手作業でラングルに貼り付けるための正しいVEX*テキスト*を提案します。AIがそれを実行することは決してありません。

  意図は**AI支援・人間ゲート**です：AIは、ラングルが適切なツールであるときにはいつでもそれを*浮かび上がらせ*、`vex_reference`に基づいた提案を差し出すよう導かれます。ユーザーが判断し実行します（検証済み経路を有効にするか、手作業で貼り付けるかのいずれか）。それこそが*安全*なVEXの要点です — すべての競合が未検証のVEX/exec（すなわちRCE）を出荷する中で、安全なのです。

- **ゲートウェイは境界ではない：** ゲートウェイは検証して転送しますが、セキュリティ保証はエグゼキューターの固定機能集合から生まれます。完全に侵害されたクライアントや誤動作するクライアントであっても、実在するハンドラーを、検証を通過する引数で呼び出すことしかできません。

- **パスの制限は二重に強制される：** ゲートウェイとエグゼキューターの両方が、すべてのパスを作業ディレクトリに`realpath`で制限します。これは意図的な冗長性です — エグゼキューターはゲートウェイが実行されたと決めつけないため、ループバックポートへの直接呼び出しであっても依然として制限されます。

- **数値パラメーターはクランプされ、拒否されない：** 範囲外の数値はエラーになるのではなく、宣言された範囲に固定されます。これにより、呼び出しを使用可能なまま保ちつつ、極端な値によるリソース枯渇を回避します。

- **ループバック＋共有トークン：** エグゼキューターはループバックのみでリッスンし、共有セッショントークンで認証します。これは、ゲートウェイとエグゼキューターが1台のマシン上で1つの信頼ドメインを共有することを前提としています。**フォローアップ：** トークンは、同一ホスト上の他のプロセスがエンドポイントを推測することから保護しますが、信頼ドメインをまたぐ制御ではありません — ループバックチャネルを、相互に不信な当事者間の境界として扱わないでください。

- **エグゼキューターPythonはディスクからロードされる：** エグゼキューターモジュールはarm時にディスクから読み込まれ、まだハッシュでピン留めされておらず、ゲートウェイバイナリに焼き込まれてもいません。すでにインストール先に書き込める ローカルの攻撃者は、これを改変できてしまいます。**フォローアップ：** エグゼキューターを既知のダイジェストにピン留めする（またはバイナリに埋め込む）ことが計画されています。それまでは、インストールディレクトリへの書き込みアクセスをコード実行と同等のものとして扱ってください。

- **シングルユーザーのローカルスコープ：** このツールは、自分自身のマシン上で1つのHoudiniセッションを操作する1人の所有者向けに設計されています。マルチテナントまたはネットワークに公開された体制では、加えてOS/コンテナのサンドボックスとエグレスポリシーが必要になります — ローカルでシングルユーザーのツールの範囲外です。ゲートウェイやエグゼキューターのループバックポートを、信頼できないネットワークアクセスに公開しないでください。
